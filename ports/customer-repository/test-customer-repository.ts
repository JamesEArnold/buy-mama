import { CustomerRepository } from '@/ports/customer-repository';

const createCustomer = async () => {

  // Push the customer into an in-memory array
};

export const testCustomerRepository: CustomerRepository = {
  createCustomer,
};
